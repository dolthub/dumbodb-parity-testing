# Collation matrix divergences: (cell, observable) pairs where DumboDB differs from MongoDB.
# Generated by TestCollationMatrix with UPDATE_MATRIX=1. 0 diverging (cell, observable) pairs.
# Key: locale|strength|caseLevel|caseFirst|numericOrdering|alternate|maxVariable|normalization|backwards|observable(order|eq)
